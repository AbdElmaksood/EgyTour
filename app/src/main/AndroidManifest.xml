<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohamed.egytour">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity2">
            android:label="Choose from"


        </activity>
        <activity android:name=".Locations.BeachesActivity" />
        <activity android:name=".Locations.RestaurantsActivity" />
        <activity android:name=".Locations.MonumentsActivity" />
        <activity android:name=".Locations.CafesActivity"></activity>
    </application>

</manifest>